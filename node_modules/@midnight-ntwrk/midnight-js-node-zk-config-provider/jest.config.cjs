const date = new Date().toISOString().replace(/:/g, '_');

module.exports = {
  preset: 'ts-jest/presets/default-esm',
  testEnvironment: 'node',
  verbose: true,
  roots: ['<rootDir>'],
  modulePaths: ['<rootDir>'],
  testPathIgnorePatterns: ['node_modules', 'dist'],
  testMatch: ['**/test/**/*.test.ts'],
  transform: {
    '^.+\\.tsx?$': [
      'ts-jest',
      {
        useESM: true,
      },
    ],
  },
  collectCoverage: true,
  coverageDirectory: 'coverage',
  coverageReporters: [
    'clover', 'json', 'json-summary', 'lcov', 'text'
  ],
  reporters: [
    'default',
    ['jest-junit', { outputDirectory: `reports/report_${date}`, outputName: 'test-report.xml' }],
    ['jest-html-reporters', { publicPath: `reports/report_${date}`, filename: 'test-report.html' }]
  ],
};
